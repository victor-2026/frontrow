appId: app.frontrow.qa
---
- runFlow: ../_setup.yaml

- tapOn:
    id: 'tab.myTickets'
- assertVisible:
    id: 'screen.myTickets'

- tapOn:
    id: 'myTickets.item.tkt_001'
- extendedWaitUntil:
    visible:
      id: 'screen.ticketDetail'
    timeout: 5000

- extendedWaitUntil:
    visible: 'Cancel ticket'
    timeout: 5000
- tapOn: 'Cancel ticket'

# Confirm dialog — iOS: text, Android: testID (RN Modal)
- runFlow:
    when:
      platform: iOS
    commands:
      - extendedWaitUntil:
          visible: 'Cancel ticket'
          timeout: 5000
      - tapOn: 'Cancel ticket'
- runFlow:
    when:
      platform: Android
    commands:
      - extendedWaitUntil:
          visible:
            id: 'ticketDetail.cancelConfirmDialog'
          timeout: 3000
      - tapOn:
          id: 'ticketDetail.cancelConfirmYes'

- assertVisible: 'refund pending'
